# Semantic Layer Production Configuration
# Copy this file to .env and configure for your environment

# === Semantic Layer Configuration ===
SEMANTIC_MODELS_PATH=semantic_models/metrics.yml

# === Server Configuration ===
SERVER_HOST=0.0.0.0
SERVER_PORT=8000
LOG_LEVEL=INFO

# === Database Connection (for production use) ===
# Uncomment and configure based on your warehouse

# DuckDB (default for development)
DATABASE_TYPE=duckdb
DATABASE_PATH=./data/sample.duckdb

# Snowflake
# DATABASE_TYPE=snowflake
# SNOWFLAKE_ACCOUNT=your-account
# SNOWFLAKE_USER=your-user
# SNOWFLAKE_PASSWORD=your-password
# SNOWFLAKE_DATABASE=PROD_DB
# SNOWFLAKE_WAREHOUSE=ANALYTICS_WH
# SNOWFLAKE_SCHEMA=PUBLIC

# BigQuery
# DATABASE_TYPE=bigquery
# BIGQUERY_PROJECT=your-project
# BIGQUERY_CREDENTIALS_PATH=./credentials.json

# PostgreSQL
# DATABASE_TYPE=postgres
# POSTGRES_HOST=localhost
# POSTGRES_PORT=5432
# POSTGRES_DATABASE=analytics
# POSTGRES_USER=your-user
# POSTGRES_PASSWORD=your-password

# === Cache Configuration ===
CACHE_BACKEND=memory  # memory, redis, file
CACHE_TTL=1800        # 30 minutes
REDIS_URL=redis://localhost:6379/0

# === Slack Integration ===
SLACK_ENABLED=false
SLACK_BOT_TOKEN=xoxb-your-bot-token
SLACK_SIGNING_SECRET=your-signing-secret
SLACK_APP_TOKEN=xapp-your-app-token
SLACK_SOCKET_MODE=false

# === Feature Toggles ===
ENABLE_WEB_API=true
ENABLE_VISUALIZATIONS=true
ENABLE_CONVERSATIONAL=true

# === Security ===
CORS_ORIGINS=https://yourdomain.com,http://localhost:3000  # Comma-separated allowed origins
API_KEY_ADMIN=your-secure-admin-api-key-here  # Generate with: openssl rand -base64 32
API_KEY_QUERY=your-secure-query-api-key-here  # Generate with: openssl rand -base64 32
API_KEY_READ=your-secure-read-api-key-here    # Generate with: openssl rand -base64 32
JWT_SECRET=your-jwt-secret-here                # Generate with: openssl rand -base64 64
ALLOWED_HOSTS=yourdomain.com,localhost         # Comma-separated trusted hosts

# === Database Security ===
POSTGRES_PASSWORD=your-secure-db-password     # REQUIRED for PostgreSQL
REDIS_PASSWORD=your-redis-password            # Optional Redis authentication
GRAFANA_PASSWORD=your-grafana-admin-password  # REQUIRED for monitoring
GRAFANA_USER=admin                            # Grafana admin username

# === Claude Desktop MCP ===
MCP_SERVER_PATH=/path/to/knowDB/src/mcp_server.py

# === AI/ML Configuration ===
OPENAI_API_KEY=your-openai-key  # For conversational metrics
# ANTHROPIC_API_KEY=your-anthropic-key  # Alternative AI provider

# === Monitoring & Logging ===
LOG_TO_FILE=false
LOG_FILE_PATH=semantic_layer.log

# === Development Settings ===
DEBUG=false
RELOAD=false  # Auto-reload on code changes (development only)
